const charechter="mario";
console.log(charechter);
const inputs =document.querySelectorAll('input');
 inputs.forEach(input => {
     console.log(input);
 });
 let a:string;
 let b:object;
 let c:(string)
 let mixed:any[]=[];
 mixed.push('one');
 mixed.push(26);
 mixed.push(true);
 mixed.push({name:"ikram", age:26});
 console.log("mixed",mixed)